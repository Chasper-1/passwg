# 🚀 PASSWG: Extreme-Performance SIMD Generator

[English Description](README) | [Описание на русском](README_RU)

**PASSWG** — это низкоуровневый генератор паролей на Rust, спроектированный для достижения максимально возможной пропускной способности данных на архитектуре x86_64.

## 🎯 Почему это быстро?
Программа игнорирует стандартные медленные методы генерации строк и работает напрямую с регистрами процессора:
- **AVX2 / SIMD**: За один такт процессора генерируется и мапится сразу 32 символа.
- **Lock-Free Parallelism**: Благодаря библиотеке `Rayon` нагрузка распределяется по всем ядрам (P-cores и E-cores) без задержек на блокировки.
- **Zero Modulo Bias**: Использование алгоритма Rejection Sampling гарантирует идеальную математическую энтропию (~6.52 бит на символ).

## 📊 Бенчмарки (Intel i3-12100f)
На бюджетном 4-ядерном процессоре PASSWG выдает следующие показатели:
- **Скорость**: ~485 000 000 паролей/сек (20 симв.)
- **Пропускная способность**: ~10.2 ГБ/сек
- **Энтропия**: ~130 бит для 20-символьного пароля.

## 🛠 Возможности
- **Три режима ChaCha**: Выбор между ChaCha8, 12 или 20 раундами (`-r`).
- **Режим Fast (`-f`)**: Максимальная оптимизация под наборы символов `[A-Za-z0-0_-]`.
- **Режим слов (`-w`)**: Генерация читаемых фраз.
- **Форматы**: Plain text, JSON, CSV.
- **Clipboard**: Прямая вставка в буфер обмена Wayland (`-c`).

## Использование
`passwg [количество символов/слов] [коливоство паролей] [флаги] [остальное для флагов]`

Если у вас есть желание генериорвать пароль в словах, всё ровно то же самое. `passwg 20 1 -w` Но расположение не фиксированное, можно написать и так `passwg -w 20 1` результат будет тот же.
Поэтому, старайтесь придерживаться стандарта, что я сделал выше. Для флага по типу `-w` не требуется указывать количество слов и паролей, они указаны в начале и автоматически заменяться с символов на слова при активно флаге.

Выводить можно как в формате `--json` или `--csv` так и чистым выводом пароля без флагов. Для записи в файл, обязательно указать `-o` и так же путь к файлу. 
Есть его не существует, он будет создан автоматически. **ВАЖНО. Необходимо вручную ставить .json или .csv**

Есть встроенный бенчмарк, который отслеживает скорость и прочую информацию самостоятельно. Для активации используйте `-s`.

## ⚙️ Сборка
```bash
cargo build --release
```
<details>
<summary>Либо жёсткие оптимизации под ваше железо.</summary>
  
```bash
RUSTFLAGS="-C target-cpu=native -C opt-level=3" cargo b -r
```
К слову, именно на таких настройках компиляции был достигнут результат в 2 секунды.
</details>

## Важные детали.
Максимальная скорость достигается исключительно в режиме **fast mode** `-f`, поэтому если у вас СИЛЬНО медленнее генерация, проблемы могут быть: **Либо в вашем железе. Либо в методе компиляции, что выше было. Либо как раз в отсутствие включенного режима** `-f`.

Так же максимальная скорость достигалась исключительно при записи в `/dev/null`. Думаю объяснять не нужно почему.
<details>
  <summary>Для тех, кому всё же нужно объяснить.</summary>
Потому что ни один ssd в мире не сможет обработать такой поток информации, так что не стоит мучить свой.

И не стоит выводить в консоль всё, это тоже ОЧЕНЬ медленно.

</details>

# Важно!!!
Генератор активно писался с помощью нейросетей и от части по этой причине, я не хочу курировать проект. *Делайте форк, играйтесь как хотите.* Я просто поделился тем, чего может быть кому-то не хватало и как пример возможностей нейросетей.
Проект считаю завершённым, ибо он выполняет главную свою функцию, генерирует пароли. Всё остальные доработки и удобные расширения оставляю на вашу фантазию. Все ошибки и проблемы, придётся решать вам самим. Но я думаю, что работать всё будет на данный момент.
Может быть, эта основная ветка будет жива, а может и нет. В любом случае не найдейтесь на меня.

# Дополнительная информация
- По всем вопросам писать прям тут, по возможности отвечу каждому.
- Нет, это не реклама нейросетей или чего подобного. Делал для себя, вышло как мне кажется добротно, решил поделиться этим.
- Если код кривой, не правильный или ещё как-то, тогда сами сделайте его лучше. Всё просто, это уже готовая база, даже если написана не идеально.


<img width="657" height="147" alt="2026-02-13_19-45-54" src="https://github.com/user-attachments/assets/9a6a9fe5-ae34-4eaf-8769-b49733d7d47c" />

# Конец!
